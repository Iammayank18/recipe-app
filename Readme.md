# React Recipe App

## steps to install and configure

1. git clone https://github.com/Iammayank18/recipe-app.git
2. then go in `recipe-app` folder and run command `yarn install`
3. then run the app with `yarn dev`
4. app will run on `http://localhost:5173/`;
